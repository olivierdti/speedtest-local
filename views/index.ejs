<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Speedtest Local</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" />
  <style>
    body { background: #0a0a0a; }
    .gauge-progress { transition: stroke-dashoffset 0.3s ease-out; }
    #needle { transition: transform 0.5s cubic-bezier(0.4, 0, 0.2, 1); transform-origin: 100px 100px; }
  </style>
</head>
<body class="min-h-screen flex items-center justify-center p-6">
  <div class="w-full max-w-4xl">
    <!-- Header -->
    <header class="text-center mb-12">
      <h1 class="text-4xl font-black text-white tracking-wider mb-2">
        SPEEDTEST<span class="text-emerald-400">LOCAL</span>
      </h1>
      <p class="text-gray-500 text-sm uppercase tracking-widest">Testez votre vitesse</p>
      
      <% if (isLocalhost) { %>
      <div class="mt-6 bg-yellow-500/10 border border-yellow-500/30 rounded-xl p-4 max-w-2xl mx-auto">
        <div class="flex items-start gap-3">
          <i class="fas fa-exclamation-triangle text-yellow-500 text-xl mt-1"></i>
          <div class="text-left">
            <div class="text-yellow-400 font-semibold mb-1">Accès Local Détecté</div>
            <p class="text-sm text-gray-300 leading-relaxed">
              Vous accédez au speedtest depuis le même PC que le serveur. Pour tester votre réseau local correctement, 
              accédez au speedtest depuis un <strong>autre appareil</strong> du réseau en utilisant :
            </p>
            <div class="mt-3 bg-black/30 rounded-lg px-4 py-2 font-mono text-emerald-400 text-sm">
              http://<%= serverIP %>:3000
            </div>
          </div>
        </div>
      </div>
      <% } %>
    </header>

    <!-- Main Content -->
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 mb-8">
      
      <!-- Left: Stats -->
      <div class="space-y-4">
        <div class="bg-zinc-900 border border-zinc-800 rounded-2xl p-6 hover:border-zinc-700 transition-colors">
          <div class="flex items-center gap-4">
            <div class="w-12 h-12 bg-emerald-500/10 rounded-xl flex items-center justify-center">
              <i class="fas fa-download text-emerald-400 text-xl"></i>
            </div>
            <div class="flex-1">
              <div class="text-xs text-gray-500 uppercase tracking-wider mb-1">Download</div>
              <div class="text-2xl font-bold text-white" id="downloadStat">—</div>
            </div>
          </div>
        </div>

        <div class="bg-zinc-900 border border-zinc-800 rounded-2xl p-6 hover:border-zinc-700 transition-colors">
          <div class="flex items-center gap-4">
            <div class="w-12 h-12 bg-blue-500/10 rounded-xl flex items-center justify-center">
              <i class="fas fa-upload text-blue-400 text-xl"></i>
            </div>
            <div class="flex-1">
              <div class="text-xs text-gray-500 uppercase tracking-wider mb-1">Upload</div>
              <div class="text-2xl font-bold text-white" id="uploadStat">—</div>
            </div>
          </div>
        </div>

        <div class="bg-zinc-900 border border-zinc-800 rounded-2xl p-6 hover:border-zinc-700 transition-colors">
          <div class="flex items-center gap-4">
            <div class="w-12 h-12 bg-purple-500/10 rounded-xl flex items-center justify-center">
              <i class="fas fa-signal text-purple-400 text-xl"></i>
            </div>
            <div class="flex-1">
              <div class="text-xs text-gray-500 uppercase tracking-wider mb-1">Ping</div>
              <div class="text-2xl font-bold text-white" id="pingStat">—</div>
            </div>
          </div>
        </div>
      </div>

      <!-- Center: Gauge -->
      <div class="lg:col-span-2 bg-zinc-900 border border-zinc-800 rounded-2xl p-8">
        <div class="relative w-full max-w-md mx-auto">
          <!-- SVG Gauge -->
          <svg class="w-full h-auto drop-shadow-2xl" viewBox="0 0 200 130">
            <defs>
              <filter id="glow">
                <feGaussianBlur stdDeviation="3" result="coloredBlur"/>
                <feMerge>
                  <feMergeNode in="coloredBlur"/>
                  <feMergeNode in="SourceGraphic"/>
                </feMerge>
              </filter>
            </defs>
            <path d="M 20 100 A 80 80 0 0 1 180 100" fill="none" stroke="#27272a" stroke-width="12" stroke-linecap="round"/>
            <path id="gaugeProgress" d="M 20 100 A 80 80 0 0 1 180 100" fill="none" stroke="#10b981" stroke-width="12" stroke-linecap="round" stroke-dasharray="251.2 251.2" stroke-dashoffset="251.2" filter="url(#glow)"/>
            <circle cx="100" cy="100" r="3" fill="#52525b"/>
            <g id="needle" style="transform-origin: 100px 100px; transform: rotate(-90deg);">
              <line x1="100" y1="100" x2="100" y2="35" stroke="#10b981" stroke-width="3" stroke-linecap="round" filter="url(#glow)"/>
              <circle cx="100" cy="100" r="5" fill="#10b981"/>
            </g>
          </svg>
          
          <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 text-center" style="margin-top: 15px;">
            <div class="text-6xl font-black text-emerald-400 drop-shadow-lg" id="speedValue">0</div>
            <div class="text-lg text-gray-500 uppercase tracking-widest mt-1">Mbps</div>
          </div>
        </div>
        
        <div class="text-center mt-4">
          <div class="text-sm text-gray-500 uppercase tracking-wider font-semibold" id="speedLabel">INACTIF</div>
        </div>

        <div class="text-center mt-8">
          <p class="text-sm text-gray-400" id="progressText">Cliquez sur GO pour commencer</p>
        </div>

        <!-- GO Button -->
        <div class="text-center mt-6">
          <% if (isLocalhost) { %>
          <button disabled class="bg-gray-600 text-gray-400 font-black text-2xl uppercase tracking-wider px-12 py-4 rounded-full cursor-not-allowed opacity-50">
            <i class="fas fa-lock mr-2"></i>BLOQUÉ
          </button>
          <p class="text-xs text-gray-500 mt-3">Test bloqué • Accédez depuis un autre appareil</p>
          <% } else { %>
          <button id="startBtn" class="bg-emerald-500 hover:bg-emerald-600 text-black font-black text-2xl uppercase tracking-wider px-12 py-4 rounded-full shadow-lg shadow-emerald-500/50 hover:shadow-emerald-500/70 transition-all hover:scale-105 active:scale-95 disabled:opacity-50 disabled:cursor-not-allowed disabled:hover:scale-100">
            GO
          </button>
          <% } %>
        </div>
      </div>
    </div>

    <!-- Details Panel -->
    <details class="bg-zinc-900 border border-zinc-800 rounded-2xl overflow-hidden">
      <summary class="px-6 py-4 cursor-pointer hover:bg-zinc-800/50 transition-colors flex items-center justify-between text-sm text-gray-400 uppercase tracking-wider font-semibold">
        <span><i class="fas fa-chart-line mr-2"></i>Détails & Logs</span>
        <i class="fas fa-chevron-down text-xs"></i>
      </summary>
      <div class="px-6 pb-6 border-t border-zinc-800">
        <div class="grid grid-cols-2 gap-4 mt-4 mb-4">
          <div class="flex justify-between items-center text-sm py-2 border-b border-zinc-800">
            <span class="text-gray-500">Meilleur Download:</span>
            <span class="text-white font-semibold" id="downloadBest">0 Mbps</span>
          </div>
          <div class="flex justify-between items-center text-sm py-2 border-b border-zinc-800">
            <span class="text-gray-500">Meilleur Upload:</span>
            <span class="text-white font-semibold" id="uploadBest">0 Mbps</span>
          </div>
        </div>
        <div class="bg-black/40 border border-zinc-800 rounded-xl p-4 max-h-48 overflow-y-auto text-xs font-mono text-emerald-400" id="log"></div>
      </div>
    </details>
  </div>

  <script>
    // Pass server info to client JS
    window.IS_LOCALHOST = <%= isLocalhost ? 'true' : 'false' %>;
  </script>
  <script src="/js/client.js"></script>
</body>
</html>
